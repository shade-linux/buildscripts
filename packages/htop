# name: htop
# pack: htop
# desc: an interactive process viewer
# deps: make, autoconf, ncurses, libtool, python3, automake
# ver: git
# source: https://github.com/htop-dev/htop

git clone ${source}
cd htop
./autogen.sh
./configure --prefix=${prefix}
make
make install
