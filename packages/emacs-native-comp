# name: gccemacs
# pack: emacs-native-comp
# desc: The extensible, customizable, self-documenting real-time display editor    
# deps: gnutls, jansson, gtk3, libgccjit
# ver: git

git clone git://git.sv.gnu.org/emacs.git
cd emacs
./autogen.sh
./configure --prefix=${prefix} --with-native-compilation
make
make install
