# name: Perl
# pack: perl
# desc: The Perl programming language
# deps: glibc, libcrypt, libxcrypt
# ver: git

git clone https://www.github.com/perl/perl5
cd perl
./configure -des -Dprefix=${prefix}/ -Dusedevel
make test
make install
