# Pre-install
cd /shade/main/cache
git clone 'https://github.com/neovim/neovim.git'
cd 'neovim'

# Build
mkdir build
cd build
cmake -DCMAKE_INSTALL_PREFIX=/shade/etc/
cmake --build build

# Install

# Post-install
