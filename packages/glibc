# name: Glibc
# pack: glibc
# desc: The GNU C library
# deps: binutils, linux-api-headers
# ver: git
# type: git
# source: https://sourceware.org/git/glibc.git

cd glibc
mkdir ../glibc-build
cd ../glibc-build
../glibc/configure --prefix=${prefix}
make
make install
