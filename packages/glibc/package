# Pre-install
git clone 'https://sourceware.org/git/glibc.git'
cd 'glibc'

# Build
mkdir ../glibc-build
cd ../glibc-build
../glibc/configure --prefix=/usr/local/shade
make

# Install
make install

# Post-install
